import 'package:flutter/material.dart';

class SwatchesScreen extends StatelessWidget {
  const SwatchesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Button');
  }
}
