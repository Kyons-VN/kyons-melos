import 'package:flutter/material.dart';

class SpaceShadowScreen extends StatelessWidget {
  const SpaceShadowScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Button');
  }
}
